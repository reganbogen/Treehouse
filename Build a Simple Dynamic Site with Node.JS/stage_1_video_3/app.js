//Problem: We need a simple way to look at a user's badge count and JavaScript point from a web browser
//Solution: Use Node.js to perfom the profile look ups and server our template via HTML

//1. Create a web server


//2. Handle HTTP route GET / and POST / i.e. Home
	//if url == "/" && Get
		//show search
	//if url -- "/" && POST
		//rediret to /:username

//3. Handle HTTP route GET / username i.e. .chalkers

	//if url =="/...."
		//get hson from Treehouse
			//on "end" 
				//show profile
			//on "error
				//show error

//4. function that hanldes that reading of files and merge in values 
	//read from title and get string
		//merge values in to string
